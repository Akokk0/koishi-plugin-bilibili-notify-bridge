# Changelog · Bilibili Notify 桥接(koishi 插件)

koishi 插件 `koishi-plugin-bilibili-notify-bridge` 的版本历史,版本号与 `package.json` 的 `version` 一致,打 `v<版本>` tag 发到 npm。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## [0.0.2] — 2026-09-21

修了一批会让桥永久断线、把内网里的东西当图发出去、或者把失败记成成功的问题;连 BN 与取图不再走 koishi 的全局代理。

⚠️ **BN 在内网、又挂在反向代理后面的主人,请看一眼反代配置。** 这一版起,桥只替 BN 取它自己取图口上的图和公网上的图。反代没设 `X-Forwarded-Proto` 的话,BN 给出的取图地址和桥接地址协议 / 端口对不上,带图的推送会失败,日志里会直说是这个原因 —— 把 `X-Forwarded-Proto` 设上就好。

### Fixed

- **Telegram 频道帖这类「没有发送者」的消息会让桥永久断线**:回传上去缺了发送者那一格,BN 当它是畸形帧断开连接,插件又把这种断开当成「再试也一样」,从此不再重连。现在拿不到发送者或群号的消息不回传,数字的账号 / 群号转成字符串照常回传;第三方适配器给了数字账号、或者不是字符串的昵称时,bot 名单也不会再被判畸形
- **失败原因太长会把桥打断线,太短又什么都不说**:OneBot 发送失败时,报错里带着整条消息连同 base64 图,9 张图的合并转发被风控拒掉一次,回执就超过 BN 的单帧上限,连接被断开、在飞的推送一起判失败;Discord、KOOK、QQ 发送失败时的原因又是空的。现在原因里的 base64 换成「[base64 图片]」、截到 1000 字,空的会说「发不出去(投递那头没说为什么)」
- **取图会替 BN 去敲内网里的任何地址**:BN 递来一条指向本机、路由器后台、云主机元数据口的地址,桥会去取、再当成图发进群,还会自动跟跳转。现在只取 BN 自己的取图口和公网上的图(解析出来的每一个地址都得是公网的),跳转自己一跳一跳跟、每一跳都重新判,最多 5 跳
- **取回来的不是图也照发**:BN 挂在要登录的反代后面时,那张登录页会被当成图发出去。现在 Content-Type 明说不是图的直接拒,字节还要按魔数再认一遍(PNG / JPEG / GIF / WEBP / BMP / AVIF / HEIC)
- **16 MB 的图片上限拦不住大文件**:以前是整个下载完才比大小。现在先看 Content-Length,再边下边数,超了当场掐断;整趟下载(连读内容在内)共用 20 秒时限
- **推送是空的也记成功**:三大适配器碰到空消息都不报错,历史里记成功、群里什么都没有。现在空推送回一条失败:「这条推送是空的(没有文字也没有图),没往群里发」
- **握完手就被踢的连接会每 0.5 秒重连一次**:以前一握上手,重连间隔就从头数。现在一次连接要稳稳跑满 60 秒,断了才从最短的间隔重新数
- **一口气来很多条带图的推送会把小机器撑爆**:以前来多少条就同时投多少条。现在最多同时投 4 条,其余排队;排队期间连接断过、或者等过了 BN 的 30 秒回执窗口的那条不再发 —— BN 那边已经判它失败了,再发会和人工重推撞车
- **插件停了、连接断了之后,还在签小程序卡的那条推送签完照样会发**:BN 已经判它失败,主人重推之后群里就是两张。现在每次往群里发之前,都再确认一次 BN 还要不要这条
- **握手那一瞬间变动的 bot 名单会丢**:koishi 是有登录事件才重报名单的,握手完成前那一下被挡掉之后,要等到下一次登录事件才补上。现在握完手会补报一次(没变就不发);重连后也不会再把一模一样的名单推两遍
- **桥的一个 bug 可能让机器人对某条消息装聋**:回传那段代码要是抛了异常,koishi 会跳过同一条消息后面的所有处理,包括 koishi 自己的指令。现在它自己接住,在日志里记一行
- **群里不带 `https://` 的 B 站链接不回传**(0.0.1 的已知问题):现在 `bilibili.com/video/BV…` 这种写法也会回传给 BN

### Changed

- **连 BN、取图都不再走 koishi 的全局代理**:koishi 配了代理时,内网里的 BN 会被送进代理而连不上,接入 token 和一次性的取图地址也会跟着进代理。BN 的地址是你直接填的,怎么连到它由这个地址说了算
- **桥接地址填错时只说一次**:以前每隔几十秒刷一行「连不上」一直重试;现在插件直接停下,在日志里说一句该改哪儿,改好之后重载插件即可。`http://` / `https://` 开头的桥接地址照样能用(koishi 会自己换成 ws)

---

## [0.0.1] — 2026-09-12

第一版。把这台 koishi 里已经配好的 bot 借给 BN:开播、动态推送经它发出去,私聊指令与群里的 B 站链接回传给 BN。协议 1.4。

需要 koishi 4.18.7 起、Node 18 起,以及一台装了「机器人框架桥接」拓展的 BN(BN 0.11.0 起,拓展市场的官方源里就有)。

⚠️ **已知问题:群里贴不带 `https://` 的 B 站链接(比如 `bilibili.com/video/BV…`)不会回传**,BN 那边也就不出卡。BN 自己认这种写法,是这个插件回传前的判据只认带 `http(s)://` 的链接。

### Added

- **形态:桥主动连 BN,BN 从不反过来连它** —— koishi 跑在内网、NAS、家宽后面都不用开端口。插件配置里填 BN 的桥接地址与接入 token 就好。**koishi 里的每个 bot 就是 BN 那边的一个 bot**,上线、下线、增删时名单当场重报,每个 bot 带着它平台的图标;BN 面板上的「测试」量得出真实往返
- **推送**:文字、图片、图文混排、多图都发得出去。@全体成员、合并转发、QQ 小程序卡只在平台做得到时才用,做不到就降级:@全体 写成一行「@全体成员」文字,合并转发拆成一张张图,小程序卡签不下来就发成标题 + 简介 + 链接的文字。markdown 报的是不支持,BN 会先把它剥成纯文本再发。**每一条发不出去的推送都带着原因回到 BN 的推送历史**
- **能力按平台报,而且只报查过的**(翻过那个平台适配器的消息编码器才写进表里):onebot 能真 @全体、真发合并转发(聊天记录卡);discord、kook 能 @全体;telegram、qq 这两样都报不支持;其余平台报「还不知道」。小程序卡只在 onebot 上探:每个 bot 上线时探一次,探不出结论就保持「还不知道」,下次再探
- **图由桥自己下载好再发**,同一条推送里的几张图一起下载;只取 http / https,单张上限 16 MB、20 秒超时。有一张取不到,整条就不发,并说明是哪一张、为什么
- **回传**:私聊 bot 的指令,和群里带链接的消息;onebot 群里转的 B 站**分享卡**也解得出链接(单独两格报给 BN,小程序卡进来不会被回一张重复的卡)。被回复的那条原文不跟着回传;同一台 koishi 借出去的另一个 bot 发的消息也不回传 —— 不然 A 推出去的卡被 B 看见,又回传成一条链接。回传哪些由 BN 在连上时说了算
- **断了自己回来**:按退避一直重连(最长 30 秒一次);BN 90 秒一帧都没有,就当这条连接已经断了、重连。token 不对或已吊销、协议大版本对不上、接入被删、同一个 token 开了两份,以及插件自己的 bug,这几种再试也是同样的结果:桥会停下,在日志里说明白,不会拿同一条 token 一直捶 BN
